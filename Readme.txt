git init

what files don't track in version control of system
create .gitignore

crate .gitattributes

git add .

git status

git commit -m "initial unity project"

git log

create repository at github

git remote add origin git@github.com:btcgames/MirrorNetwork.git

git push -u origin master

